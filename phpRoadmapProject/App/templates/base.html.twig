<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}Jobeet - Your best job board{% endblock %}</title>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}{% endblock %}

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <style>
        .flash {
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            text-align: center;
            background-color: #b0e0ff;
            z-index: 2
        }
        .displayNone{
            display: none;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('job.list') }}">Jobeet</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div>
                        <p>{{ app.user.email }}</p>
                    </div>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div>
                        <a href="{{ path('job.create') }}" class="btn btn-default navbar-btn">Post a Job</a>
                    </div>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
            <li>
                <div>
                    <a href="{{ path('job.export', {format: 'json', id: 1})}}" class="btn btn-default navbar-btn">export a Job in json</a>
                </div>
            </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div>
                        <a href="{{ path('job.export', {format: 'csv', id: 1})}}" class="btn btn-default navbar-btn">export a Job in csv</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>

{% for message in app.flashes('notice') %}
    <div class="flash">
        {{ message }}
    </div>
{% endfor %}
</body>
</html>