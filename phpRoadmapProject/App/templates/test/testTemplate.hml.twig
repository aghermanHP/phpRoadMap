<!DOCTYPE html>
<html lang="en">
<head>
    <title>CSS Template</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        * {
            box-sizing: border-box;
            margin: unset;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
        }

        /* Style the header */
        header {
            background-color: #777;
            text-align: center;
            margin: 0;
            color: white;
            position: fixed;
            width: 100%;
            height: 10%;
            display: inline;
            z-index: 2;
        }

        nav {
            float: left;
            width: 20%;
            background: #ccc;
            padding: 20px;
            top:10%;
            position: fixed;
            height: 100%;
        }

        nav ul {
            list-style-type: none;
            padding: 0;
        }

        article {
            float: right;
            padding: 20px;
            position: relative;
            margin-top: 5%;
            height: 100%;
            width: 79%;
            background-color: #f1f1f1;
        }

        /* Style the footer */
        footer {
            background-color: #777;
            padding: 10px;
            text-align: center;
            color: white;
            bottom: 0;
            position: fixed;
            width: 100%;
        }
    </style>
</head>
<body>
<header>
    <h2>Cities</h2>
</header>

<section>
    <nav>
        <ul>
            <li><a href="#">{{ test_variable|upper }}</a></li>
            <li><a href="#">{{ test_variable|lower }}</a></li>
            <li><a href="#">{{ test_variable|reverse }}</a></li>
            <li><a href="#">{{ test_variable|capitalize }}</a></li>
            <li><a href="#">{{ test_variable|escape }}</a></li>
            <li><a href="#">{{ test_variable|length }}</a></li>
        </ul>
    </nav>

    <article>
        {% block body %}{% endblock %}
        {{ include('test/test.html.twig') }}
        {{ include('/job/newJob.html.twig') }}
    </article>


</section>

<footer>
    <p>Footer</p>
</footer>

</body>
</html>
